<h1><%= @target.description %></h1>

<table class="table table-bordered">
  <% @target.tasks.each do |task| %>
    <tr>
      <td>
        <%= task.description %>
      </td>
    </tr>
  <% end -%>
</table>

<p>  </p>

<%= link_to '修改', edit_target_path(@target), class: 'btn btn-primary btn-block' %>
